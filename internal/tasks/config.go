package tasks

// Config holds configuration for the tasks module.
//
// Currently, this module does not require any specific configuration.
// The struct is provided for consistency with other modules and future
// extensibility (e.g., default limits, feature flags).
type Config struct {
}
